{% comment %}
247 Print Network - Collection Header Section
Premium header for collection pages
{% endcomment %}

{{ '247-art.css' | asset_url | stylesheet_tag }}

<section class="collection-header">
  <div class="collection-header__container">
    <div class="collection-header__content">
      {% if collection.image %}
        <div class="collection-header__image">
          <img 
            src="{{ collection.image | img_url: '1200x400', crop: 'center' }}" 
            alt="{{ collection.title }}"
            loading="eager"
          >
        </div>
      {% endif %}
      
      <div class="collection-header__text">
        <h1 class="collection-header__title">{{ collection.title }}</h1>
        {% if collection.description != blank %}
          <div class="collection-header__description">{{ collection.description }}</div>
        {% endif %}
        <div class="collection-header__count">{{ collection.products_count }} artworks</div>
      </div>
    </div>
  </div>
</section>

<style>
.collection-header {
  background: linear-gradient(135deg, #0f0f0f 0%, #1a1a1a 100%);
  padding: 4rem 2rem 3rem;
  position: relative;
  overflow: hidden;
}

.collection-header::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: 
    radial-gradient(circle at 20% 50%, rgba(99, 102, 241, 0.15) 0%, transparent 50%),
    radial-gradient(circle at 80% 50%, rgba(99, 102, 241, 0.1) 0%, transparent 50%);
  pointer-events: none;
}

.collection-header__container {
  max-width: 1400px;
  margin: 0 auto;
  position: relative;
  z-index: 1;
}

.collection-header__content {
  text-align: center;
}

.collection-header__image {
  margin-bottom: 2rem;
  border-radius: 16px;
  overflow: hidden;
  max-width: 800px;
  margin-left: auto;
  margin-right: auto;
}

.collection-header__image img {
  width: 100%;
  height: auto;
  display: block;
}

.collection-header__title {
  font-size: 3.5rem;
  font-weight: 800;
  color: #fff;
  margin: 0 0 1rem;
  letter-spacing: -0.03em;
  line-height: 1.1;
}

.collection-header__description {
  font-size: 1.25rem;
  color: rgba(255, 255, 255, 0.8);
  line-height: 1.7;
  max-width: 700px;
  margin: 0 auto 1rem;
}

.collection-header__count {
  font-size: 0.9375rem;
  color: rgba(255, 255, 255, 0.6);
  text-transform: uppercase;
  letter-spacing: 0.1em;
  font-weight: 600;
}

@media (max-width: 768px) {
  .collection-header {
    padding: 3rem 1.5rem 2rem;
  }

  .collection-header__title {
    font-size: 2.5rem;
  }

  .collection-header__description {
    font-size: 1.125rem;
  }
}
</style>

{% schema %}
{
  "name": "247 Collection Header",
  "presets": [
    {
      "name": "247 Collection Header"
    }
  ]
}
{% endschema %}
